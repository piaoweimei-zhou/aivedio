# 导演工作台 — Python 依赖
# 注：pydantic-core 2.14.1 无 Python 3.13 wheel（需 Rust 编译），用 >=2.40 版
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
python-dotenv>=1.0.0
httpx>=0.25.0
aiohttp>=3.9.0
Pillow>=10.0.0
psutil>=5.9.0
python-multipart>=0.0.6

# 图像处理（template_clean_stage / comfyui_service）
opencv-python>=4.8.0
numpy>=1.24.0
PyYAML>=6.0

# 文本生成 / TTS（延迟导入，运行时需要）
openai>=1.0.0
edge-tts>=6.1.0

# 开发/测试依赖
pytest>=8.0
pytest-asyncio>=0.23
