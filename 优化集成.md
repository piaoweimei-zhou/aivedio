# Infinite-Canvas 优化集成方案（修订版）

## 修订说明

基于对原方案的深度评估，主要调整：
1. **Phase 1** 增加依赖分析前置步骤，解决 13,188 行 main.py 拆解风险
2. **Phase 2** 改为独立视频服务，不再强绑到 pipeline_executor
3. **Phase 3** 标记暂缓
4. **HTTP 客户端** 统一为 httpx

---

## 背景

将 **Infinite-Canvas**（网盘会员最新版）的供应商抽象层、视频生成、画布系统等成熟能力，集成到 **ai-ide-v2** 的 pipeline 驱动架构中，各取所长。

### 能力互补矩阵

| 领域 | ai-ide-v2 有 | Infinite-Canvas 有 | 集成价值 |
|------|-------------|-------------------|---------|
| **AI 供应商层** | 仅 ComfyUI 本地 | **7 个供应商**（OpenAI/RunningHub/ModelScope/即梦/火山/Gemini/ApiMart） | ⭐⭐⭐ |
| **视频生成** | 无 | **Veo/Sora/Wan/Seedance/Doubao 等 20+ 模型** | ⭐⭐⭐ |
| **图像生成** | ComfyUI 本地管线 | 云端 API + ComfyUI + RunningHub | ⭐⭐⭐ |
| **画布编辑** | 无 | **无限画布 + WebSocket 实时同步** | ⭐⭐ |
| **资产管理** | 基础 CRUD | 资产库分组/整理/导入导出 | ⭐⭐ |
| **工作流引擎** | 强大（pipeline/Agent） | 基础（仅 RunningHub 工作流） | ai-ide-v2 强 |
| **前端 UI** | React SPA（262 组件） | HTML 直出 | ai-ide-v2 强 |
| **代码/Agent** | 完整体系 | 无 | ai-ide-v2 强 |
| **可观测性** | 结构化日志/链路追踪 | print() | ai-ide-v2 强 |

---

## 前置风险评估

### 核心风险：Infinite-Canvas main.py 拆解

**严重度：高 | 概率：高**

`main.py`（13,188 行）中的每个函数都可能依赖：
- 全局变量（`api_providers`、`canvas_data` 等）
- 共享状态（当前画布、用户 session）
- 隐式 import（函数内部 `from ... import`）

**缓解策略：**
1. 抽取前先用静态分析工具（如 `pylint` + 手动阅读）绘制每个目标函数的完整依赖图
2. 优先抽取**纯函数**（无副作用的工具函数），再抽**有状态函数**（需 Provider 对象的）
3. 每抽一个函数就运行一次 `pytest` 确保不破坏现有功能

### 风险矩阵

| 风险 | 严重度 | 概率 | 缓解策略 |
|------|--------|------|---------|
| Infinite-Canvas 代码耦合度高，拆解困难 | 高 | 高 | 先做依赖分析，逐函数抽取 |
| httpx vs aiohttp 冲突 | 中 | 中 | 统一用 httpx，一次性迁移 aiohttp |
| 供应商 API 变更 | 中 | 低 | 抽象层隔离，每个供应商独立测试 |
| ProviderResult 与现有 ComfyUIGenResult 不兼容 | 高 | 中 | `to_comfyui_result()` 适配器 |
| 视频生成异步模型与 pipeline 同步执行冲突 | 高 | 高 | Phase 2 独立服务不绑 pipeline |

---

## Phase 1：Provider 抽象层（最高价值，3-4 天）

### 可行性：⭐⭐⭐⭐ (高)

### 新增：依赖分析前置步骤（Day 0.5）

在写任何代码之前，分析 Infinite-Canvas `main.py` 中各目标函数的依赖链：

```python
# 分析脚本: scripts/analyze_deps.py
# 对 generate_ai_image / generate_jimeng_provider_image 等目标函数：
# 1. 提取所有 import 和全局变量引用
# 2. 绘制依赖图
# 3. 标记哪些函数可单独抽取，哪些需要先拆依赖
```

输出：每个目标函数的"可抽取性"评分 + 依赖清单。

### 现有代码分析

**ai-ide-v2 的 `image_service.py`**（已有 Provider 雏形）：
```python
IMAGE_MODELS = {
    "earney":    {"provider": "baidu"},
    "sdxl":      {"provider": "openai_compat"},
    "nano_banana": {"provider": "openai_compat"},
    "comfyui_yaoguang": {"provider": "comfyui"},
}

class ImageService:
    async def generate(self, prompt_json, model, ...) -> ImageGenResult:
        if provider == "comfyui":
            result = await comfyui.generate(...)
        else:
            image_url = await self._call_api(api_url, payload, key, provider)
```

### 深度集成方案

#### Step 1：定义统一结果模型

```python
# backend/services/provider_service.py

@dataclass
class ProviderResult:
    """统一供应商结果 — 与 ComfyUIGenResult 对齐"""
    image_url: str = ""
    images: List[str] = None       # 多图
    filenames: List[str] = None
    seed: int = 0
    elapsed_ms: int = 0
    prompt: str = ""
    provider_id: str = ""          # ⭐ 标记来源供应商
    model: str = ""

    def __post_init__(self):
        if self.images is None:
            self.images = [self.image_url] if self.image_url else []
        if self.filenames is None:
            self.filenames = []

    def to_comfyui_result(self) -> "ComfyUIGenResult":
        """兼容现有 pipeline_executor 的 ComfyUIGenResult 接口"""
        from services.comfyui_service import ComfyUIGenResult
        return ComfyUIGenResult(
            image_url=self.image_url,
            filename=self.filenames[0] if self.filenames else "",
            images=self.images,
            filenames=self.filenames,
            seed=self.seed,
            elapsed_ms=self.elapsed_ms,
            prompt=self.prompt,
        )
```

#### Step 2：Provider 基类 + 供应商实现

```python
class ImageProvider(ABC):
    """图像生成供应商基类"""

    @abstractmethod
    async def generate(
        self,
        prompt: str,
        size: str = "1024x1024",
        model: str = "",
        reference_images: Optional[List[Dict]] = None,
        **kwargs
    ) -> ProviderResult:
        ...

class ComfyUIProvider(ImageProvider):
    """封装 ai-ide-v2 的 comfyui_service — 零改动"""
    async def generate(self, prompt, size="", model="", reference_images=None, **kwargs) -> ProviderResult:
        from services.comfyui_service import get_comfyui_service
        service = get_comfyui_service()
        result = await service.generate_storyboard(
            reference_items=reference_images or [],
            prompt_text=prompt,
            **kwargs
        )
        return ProviderResult(
            image_url=result.image_url,
            images=result.images,
            filenames=result.filenames,
            seed=result.seed,
            elapsed_ms=result.elapsed_ms,
            prompt=result.prompt or prompt,
            provider_id="comfyui",
        )

# 其他 Provider（从 Infinite-Canvas 提取，但先做依赖分析）
```

#### Step 3：ProviderService 路由

```python
class ProviderService:
    """统一供应商路由服务"""

    def __init__(self):
        self._providers: Dict[str, ImageProvider] = {}
        self._load_providers()

    def _load_providers(self):
        self._providers["comfyui"] = ComfyUIProvider()  # 始终可用
        if os.getenv("OPENAI_API_KEY"):
            self._providers["openai_compat"] = OpenAICompatProvider()
        if os.getenv("RUNNINGHUB_API_KEY"):
            self._providers["runninghub"] = RunningHubProvider()
        # ... 其他供应商按需加载

    async def generate(self, provider_id: str, **kwargs) -> ProviderResult:
        provider = self._providers.get(provider_id)
        if not provider:
            available = list(self._providers.keys())
            raise ValueError(f"供应商 {provider_id} 不可用，当前可用: {available}")
        return await provider.generate(**kwargs)

    def available_providers(self) -> List[Dict]:
        return [{"id": k, "name": v.__class__.__name__} for k, v in self._providers.items()]
```

#### Step 4：模板类型增加 provider 字段

```typescript
// PipelineSharedComponents.tsx — STORYBOARD_TEMPLATE_TYPES 扩展
const STORYBOARD_TEMPLATE_TYPES = [
  { type: 'costume_change', name: '分镜换装', provider: 'comfyui', ... },
  { type: 'multi_frame',    name: '多帧分镜', provider: 'comfyui', ... },
  { type: 'panorama',       name: '全景图',   provider: 'comfyui', ... },
  { type: 'pose_transfer',  name: '姿态迁移', provider: 'comfyui', ... },
]
```

#### Step 5：改造 pipeline_executor（最小改动）

```python
# pipeline_executor.py — _execute_single_shot_storyboard()
from services.provider_service import get_provider_service
provider_svc = get_provider_service()
provider_id = shot.get("provider", "comfyui")  # 默认 ComfyUI，向后兼容
result = await provider_svc.generate(
    provider_id=provider_id,
    prompt=prompt_text,
    size=f"{_shot_w or 1024}x{_shot_h or 1024}",
    reference_images=ref_items,
    **gen_kwargs,
)
```

### 需要从 Infinite-Canvas 提取的代码（按依赖分析结果优先级）

| 功能 | 位置 | 行数 | 提取条件 | 优先级 |
|------|------|------|---------|--------|
| 供应商配置管理 | L617-L1300 | ~680 | 需先分析依赖链 | ⚠️ 依赖分析后定 |
| RunningHub 图像生成 | L7881-L7965 | ~85 | 低依赖，可优先 | ✅ 高 |
| 即梦图像生成 | L4244-L4280 | ~40 | 依赖 CLI 环境 | ⚠️ 中 |
| 火山引擎图像生成 | L7034-L7050 | ~20 | 低依赖，可优先 | ✅ 高 |
| Gemini 图像生成 | L7004-L7020 | ~20 | 低依赖，可优先 | ✅ 高 |
| ModelScope 图像生成 | L6911-L6970 | ~60 | 低依赖，可优先 | ✅ 高 |
| 辅助函数 (协议/尺寸/引用) | L3350-L3650 | ~300 | 纯函数 | ✅ 最高 |

---

## Phase 2：视频生成独立服务（高价值，3-4 天）

### 可行性：⭐⭐⭐ (中)

### 关键变更：不做 pipeline 集成，改为独立视频服务

**为什么不分镜绑 pipeline：**
- pipeline_executor 是同步设计（等待结果 → 返回），视频生成是异步（提交 → 轮询）
- 强行绑在一起需要大改 pipeline 架构，性价比低
- 独立服务可以独立验证、独立部署、独立迭代

### 架构

```
视频生成请求 → VideoService.generate()
  → 提交到供应商（即梦/RunningHub/火山）
  → 返回 task_id + 初始状态
  → 前端轮询 /video/task/{task_id}
  → VideoService.query() 返回最新状态
  → 完成后返回 video_url + last_frame_url
```

### 数据模型

```python
# backend/services/video_service.py

@dataclass
class VideoRequest:
    prompt: str
    images: List[str] = None         # 参考图 URL
    model: str = ""
    duration: float = 5.0
    aspect_ratio: str = "16:9"
    provider_id: str = ""

@dataclass
class VideoResult:
    video_url: str = ""
    task_id: str = ""
    status: str = ""                 # running / succeeded / failed
    provider_id: str = ""
    last_frame_url: str = ""         # 最后一帧（可做下一帧首帧）
    elapsed_ms: int = 0
```

### API 端点

```python
# backend/api/video_api.py

@router.post("/video/generate")
async def generate_video(request: VideoRequest):
    """提交视频生成任务"""
    task = await video_service.generate(request)
    return {"task_id": task.task_id, "status": task.status}

@router.get("/video/task/{task_id}")
async def query_video_task(task_id: str):
    """轮询视频生成状态"""
    result = await video_service.query(task_id)
    return {"status": result.status, "video_url": result.video_url, ...}
```

### 前端

```tsx
// 新增 VideoPanel.tsx（独立于 ShotEditor）
const VideoPanel = () => {
  const [taskId, setTaskId] = useState('')
  // 轮询 /video/task/{taskId}
  // 完成后显示 <video> 播放器
}
```

### 需要从 Infinite-Canvas 提取的代码

| 功能 | 位置 | 行数 |
|------|------|------|
| 即梦视频流程（CLI 提交 + 轮询） | L4283-L4398 | ~115 |
| RunningHub 视频流程 | L8040-L8100 | ~60 |
| 火山引擎视频参数 | L6800-L6830 | ~30 |
| 视频辅助函数 | L5709-L5830 | ~120 |

---

## Phase 3：画布系统（暂缓）

### 可行性：⭐⭐ (低)

**决定：不纳入本次实施计划**

理由：
1. React 重写工作量（5-7天）与价值不匹配
2. iframe 方案有跨域和服务共存问题
3. 当前阶段应聚焦 Provider + 视频

**将来如果实施：**
- 建议方案 B（React 重写），50% 时间做后端 API 复用，50% 做前端画布组件

---

## Phase 4：工具函数（随用随抽）

### 可行性：⭐⭐⭐⭐⭐ (最高)

**提取策略：**
- 按需提取，每次提取后立即运行测试
- 优先提取纯函数（无副作用、无全局依赖）
- 提取路径：`backend/utils/`

**优先提取列表（纯函数，无需依赖分析）：**

| 函数 | 用途 | Infinite-Canvas 位置 |
|------|------|---------------------|
| `reference_to_data_url()` | 图片→data URL 编码 | L5572 |
| `compress_data_url_image()` | 压缩 data URL 图片 | L5829 |
| `output_path_for()` | 输出文件路径解析 | L4465 |
| `mask_secret()` | API Key 脱敏 | L682 |
| `extract_image_flexible()` | 灵活提取图片 URL | L3448 |
| `image_has_alpha()` | 检测 PNG 透明通道 | L4490 |
| `parse_size_pair()` | 尺寸字符串解析 | L6660 |

---

## 完整架构

```
┌─────────────────────────────────────────────────────────────────┐
│                     ai-ide-v2 统一平台                           │
│                                                                 │
│  pipeline_executor（同步图像）       VideoService（异步视频）    │
│  ┌──────────────────────┐     ┌──────────────────────────┐     │
│  │   ProviderService    │     │    VideoService          │     │
│  │  ┌────┬────┬──────┐  │     │  ┌────┬────┬─────────┐  │     │
│  │  │Comfy│RunHub│火山 │  │     │  │即梦│RunHub│火山方舟│  │     │
│  │  ├────┼────┼──────┤  │     │  ├────┼────┼─────────┤  │     │
│  │  │Gemini│MS  │即梦 │  │     │  │APIMart│... │        │  │     │
│  │  └────┴────┴──────┘  │     │  └────┴────┴─────────┘  │     │
│  └──────────────────────┘     └──────────────────────────┘     │
│                │                               │               │
│  ┌─────────────▼───────────────────────────────▼───────────┐   │
│  │           统一结果模型 ProviderResult / VideoResult       │   │
│  └─────────────────────────────────────────────────────────┘   │
│                │                                               │
│  ┌─────────────▼───────────────────────────────────────────┐   │
│  │              前端 React SPA                              │   │
│  │  ShotEditor | VideoPanel | AssetManager                │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │              共享基础设施                                 │   │
│  │  结构化日志 | httpx 统一客户端 | .env 配置 | 工具函数库    │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

---

## 基础设施统一

### HTTP 客户端：统一为 httpx

**方案：一次性迁移，不混用**

```python
# 现状：aiohttp + httpx 混用
# 目标：全部使用 httpx

# 迁移步骤：
# 1. 创建 httpx 统一 session 管理
# 2. 逐个文件替换 aiohttp → httpx
# 3. 每个文件替换后运行全量测试

# httpx 相比 aiohttp 的优势：
# - 支持 HTTP/2（供应商 API 越来越多要求 HTTP/2）
# - API 更简洁（response.raise_for_status() 等）
# - 流式响应原生支持
# - 连接池管理更完善
```

---

## 实施路线图（修订版）

```
Phase 1 (4天) → Provider 抽象层
├── Day 0.5: Infinite-Canvas main.py 依赖分析（关键前置）
├── Day 0.5: 定义 ProviderResult + ImageProvider 基类 + STORYBOARD_TEMPLATE_TYPES 扩展 provider 字段
├── Day 1:   ComfyUIProvider + 提取纯函数工具类
├── Day 1:   提取 OpenAICompatProvider + RunningHubProvider + pipeline_executor 适配
├── Day 1:   提取 JimengProvider + VolcEngineProvider + GeminiProvider
└── 验收: pipeline_executor 可按模板选择供应商生成图像

Phase 2 (独立, 3-4天) → 视频生成独立服务
├── Day 1:   定义 VideoRequest/VideoResult + API 端点
├── Day 1:   JimengVideoProvider（CLI 提交 + 轮询）
├── Day 1:   RunningHubVideoProvider
├── Day 1:   前端 VideoPanel.tsx（独立于 ShotEditor）
└── 验收: 可通过 API 提交视频生成任务并轮询获取结果

基础设施 (穿插进行)
├── 统一的 httpx 客户端替换 aiohttp
└── 工具函数按需提取到 backend/utils/

Phase 3 (暂缓) → 画布系统

Phase 4 (随用随抽) → 工具函数
```

---

## 前置依赖

### 必须确认

1. **供应商 API Key**：
   - RunningHub API Key
   - 即梦（Jimeng）CLI 环境
   - 火山引擎 AccessKey/SecretKey
   - APIMart / 其他 OpenAI 兼容供应商

2. **配置方式**：统一用 `.env`（与 ai-ide-v2 一致）

3. **HTTP 客户端**：统一用 httpx，一次性迁移

4. **依赖分析脚本**：实施 Phase 1 前完成对 Infinite-Canvas main.py 的静态分析
