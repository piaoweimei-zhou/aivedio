# 制作模版 — 三阶段工业化流程

## 概览

模板制作分为 3 个阶段，从参考构图图出发，逐步生成可复用的模板资产（Pose / 深度 / 线稿 / 蒙版）。每个阶段对应一个后端 Stage 和一个 ComfyUI 工作流，产出的文件自动按规范命名并存入 `workflows/templates/` 目录，同时更新 `templates_manifest.json`。

```
参考构图图 ──→ Phase 1: 批量提取 ──→ Phase 2: 清场+蒙版 ──→ Phase 3: Pose优化
                  │                      │                      │
                  ├─ pose.png            ├─ depth_clean.png     ├─ pose_simplified.png
                  ├─ depth_raw.png       └─ mask.png            └─ (可选) 手动修正
                  └─ lineart.png
```

---

## Phase 1：模板批量提取

**Stage ID**：`template_batch_extract`
**工作流文件**：`workflows/三个骨架图.json`
**输入**：参考构图图（concept / storyboard 类型）
**输出**：Pose 骨架图 + 深度图 + 线稿图（三件套）

### 工作流细节

| 节点 | 功能 | 说明 |
|------|------|------|
| `LoadImage` | 加载参考构图图 | 输入源图 |
| `LayerUtility: ImageScaleByAspectRatio V2` | 等比缩放 | 最长边 1920px，lanczos 插值，crop 模式 |
| `DWPreprocessor` | DWPose 骨架检测 | 检测 body + hand + face，使用 yolox_l + dw-ll_ucoco 模型 |
| `AIO_Preprocessor (DepthAnythingV2Preprocessor)` | 深度图提取 | DepthAnythingV2 模型 |
| `AIO_Preprocessor (LineArtPreprocessor)` | 线稿提取 | LineArt 预处理器 |
| `SaveImage` | 保存三图 | prefix 分别为 pose / shendu / xiangao |

### 输出文件命名

| 产出 | 文件名 | 资产类型 |
|------|--------|----------|
| Pose 骨架图 | `{template_id}_pose.png` | `pose` |
| 深度图 | `{template_id}_depth_raw.png` | `depth` |
| 线稿图 | `{template_id}_lineart.png` | `lineart` |

### 关键逻辑

- **幂等性**：重新提取时自动清除上次生成的资产记录和下游产物（清场深度图、蒙版、简化Pose），因为原始图变了下游产物已失效
- **manifest 状态**：三图齐全 → `extracted`，部分 → `partial`，无 → `pending`
- **安全文件名**：ComfyUI 的 `filename_prefix` 使用 `safe_filename_prefix()` 将中文替换为 ASCII，避免 Windows 中文文件名乱码

---

## Phase 2：模板清场 + 蒙版生成

**Stage ID**：`template_clean`
**工作流文件**：`workflows/模板清场+蒙版.json`
**输入**：参考构图图 + 原始深度图（Phase 1 产出，可选）
**输出**：清场深度图 + 最终蒙版

### 工作流细节

| 节点 | 功能 | 说明 |
|------|------|------|
| `LoadImage` (node 1) | 加载参考构图图 | 用于 SAM2 人物检测 |
| `LoadImage` (node 2) | 加载原始深度图 | 用于 Inpaint 清场 |
| `SAM2ModelLoader` | 加载 SAM2 模型 | `sam2_hiera_large.pt` |
| `GroundingDinoModelLoader` | 加载 GroundingDINO | `SwinT_OGC` 模型 |
| `GroundingDinoSAM2Segment` | 人物检测+分割 | prompt: `person . people . human`，threshold: 0.3 |
| `GrowMask` | 蒙版收缩 | expand: -1（收缩 1 像素，防止边缘残留） |
| `FeatherMask` | 蒙版羽化 | 3 像素高斯模糊，实现平滑过渡 |
| `InvertMask` | 反转蒙版 | 人物区域 → 清场区域 |
| `MaskToImage` + `SaveImage` | 保存最终蒙版 | `mask_final` |
| `UNETLoader` | 加载 Qwen Image Edit | `qwen_image_edit_2511_fp8_fix` |
| `LoraLoaderModelOnly` | 加载 LoRA | `Qwen-Image-Edit-F2P一致性`，strength: 0.25 |
| `TextEncodeQwenImageEditPlus` | 正向编码 | "Remove all people... Fill with smooth natural depth gradients" |
| `KSampler` | 深度图清场采样 | 4 步，euler_ancestral，beta57 调度器 |
| `VAEDecode` + `SaveImage` | 保存清场深度图 | `depth_clean` |

### 蒙版处理流程

```
SAM2 人物分割 → 人物蒙版
    ├─ GrowMask(-1px) → FeatherMask(3px) → MaskToImage → 保存为 mask_final
    └─ InvertMask → 清场区域（用于 Inpaint）
```

### 输出文件命名

| 产出 | 文件名 | 资产类型 |
|------|--------|----------|
| 清场深度图 | `{template_id}_depth_clean.png` | `depth_clean` |
| 最终蒙版 | `{template_id}_mask.png` | `mask` |

### 关键逻辑

- **深度图自动关联**：如果只传了参考构图图，会自动从同项目查找 Phase 1 生成的深度图
- **蒙版后处理**：1 像素收缩 + 3 像素羽化，确保蒙版边缘平滑无残留
- **Inpaint 模型**：使用 Qwen Image Edit + F2P 一致性 LoRA，4 步快速清场
- **幂等性**：重新清场时自动清除旧资产记录
- **manifest 状态**：有 depth_clean 或 mask → `cleaned`，全部文件齐全 → `ready`

---

## Phase 3：模板 Pose 优化

**Stage ID**：`template_pose`
**工作流文件**：`workflows/模板Pose优化.json`
**输入**：原始 Pose 骨架图（Phase 1 产出）
**输出**：简化 7 节点骨架图

### 工作流细节

| 节点 | 功能 | 说明 |
|------|------|------|
| `LoadImage` | 加载原始 Pose 图 | Phase 1 产出的完整 OpenPose 骨架 |
| `SimplifiedPoseRenderer` | 简化骨架渲染 | 只保留 7 个关键节点 |
| `SaveImage` | 保存简化 Pose | `pose` prefix |

### SimplifiedPoseRenderer 参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `joint_radius` | 5 | 关节点圆半径 |
| `line_width` | 3 | 骨骼连线宽度 |
| `head_radius` | 8 | 头部圆半径 |
| `background_color` | black | 背景色 |

### 简化策略

完整 OpenPose 骨架包含 100+ 关节点（含手指、面部等），简化后只保留：
- **头**（1 个大圆）
- **肩**（左右 2 点）
- **肘**（左右 2 点）
- **胯**（左右 2 点）
- **膝**（左右 2 点）
- **脚**（左右 2 点）

### 输出文件命名

| 产出 | 文件名 | 资产类型 |
|------|--------|----------|
| 简化 Pose | `{template_id}_pose_simplified.png` | `pose` |

### 手动修正（PoseEditor）

简化 Pose 生成后，可通过前端「Pose手动编辑」功能进行交互式修正：

- **黑笔覆盖**：用黑色画笔覆盖错误的关节点
- **白笔修正**：用白色画笔画出正确的关节位置
- **笔刷大小**：2-30 可调
- **撤销/重做/清除**：支持操作历史
- **保存**：导出修正后的 PNG，上传后端并更新 manifest

修正后的文件保存为 `{template_id}_pose_corrected.png`，manifest 中 `pose_corrected: true`。

---

## 模板资产清单

完成 3 个阶段后，每个模板在 `workflows/templates/` 目录下拥有以下文件：

```
workflows/templates/
├── {template_id}_pose.png              # Phase 1: 原始 Pose 骨架
├── {template_id}_depth_raw.png         # Phase 1: 原始深度图
├── {template_id}_lineart.png           # Phase 1: 线稿图
├── {template_id}_depth_clean.png       # Phase 2: 清场深度图
├── {template_id}_mask.png              # Phase 2: 最终蒙版
├── {template_id}_pose_simplified.png   # Phase 3: 简化 Pose
├── {template_id}_pose_corrected.png    # Phase 3: 手动修正 Pose（可选）
└── templates_manifest.json             # 模板清单索引
```

## Manifest 状态流转

```
pending → partial → extracted → cleaned → ready
                        ↑          ↑         ↑
                   Phase 1 完成  Phase 2   Phase 1+2+3 全部完成
```

| 状态 | 含义 |
|------|------|
| `pending` | 无任何产出文件 |
| `partial` | Phase 1 部分产出 |
| `extracted` | Phase 1 三图齐全 |
| `cleaned` | Phase 2 清场/蒙版已生成 |
| `ready` | 全部文件齐全（pose + depth_raw + lineart + depth_clean + mask） |

## 推荐参数

每个模板在 manifest 中附带推荐参数（`recommended_params`），用于下游的约束生成：

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `depth_weight` | 0.8 | 深度图约束权重 |
| `pose_weight` | 0.45 | Pose 约束权重 |
| `mask_blur` | 2 | 蒙版模糊半径 |
